name: colorsing_flutter_lints
description: Custom Flutter lints for ColorSing application
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  custom_lint_builder: ^0.7.6
  analyzer: ^7.6.0
  analyzer_plugin: ^0.13.4

dev_dependencies:
  test: ^1.24.0
